<Popup x:Class="duk.App.CompletionPopup"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       AllowsTransparency="True"
       StaysOpen="False"
       Placement="Absolute">

    <Border Background="#252526" BorderBrush="#454545" BorderThickness="1"
            MaxHeight="220" MinWidth="300" MaxWidth="500">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 候補リスト -->
            <ListBox x:Name="ItemsList" Grid.Row="0"
                     Background="Transparent" BorderThickness="0"
                     FontFamily="Consolas" FontSize="13"
                     SelectionChanged="ItemsList_SelectionChanged"
                     KeyDown="ItemsList_KeyDown"
                     MouseDoubleClick="ItemsList_DoubleClick"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="22"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding KindIcon}"
                                       FontSize="11" Foreground="#569cd6"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Label}"
                                       Foreground="#d4d4d4" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding Detail}"
                                       Foreground="#6e6e6e" FontSize="11"
                                       VerticalAlignment="Center" Margin="8,0,4,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- 詳細パネル -->
            <Border Grid.Row="1" Background="#1e1e1e"
                    BorderBrush="#454545" BorderThickness="0,1,0,0"
                    Padding="8,4" x:Name="DetailBorder" Visibility="Collapsed">
                <TextBlock x:Name="DetailText" Foreground="#9cdcfe"
                           FontFamily="Segoe UI" FontSize="12"
                           TextWrapping="Wrap" MaxWidth="480"/>
            </Border>
        </Grid>
    </Border>
</Popup>
